function vankata(pagesNum, hourlyPagesRead, daysTarget){

    let totalHours = pagesNum / hourlyPagesRead;
    let numOfHoursNeeded = totalHours / daysTarget;

    console.log(numOfHoursNeeded);

}

vankata(432,

    15 ,
    
    4)